# DX report — RevenueCat Ads (beta) on React Native + AdMob SSV

Friction log for the Shipaton `#revenuecat-help` channel. Filed from the desk on 2026-09-16 (typings + docs);
device findings are appended after the G1 spike.

| #    | What                                                                                                                                                                                                                         | Where                                                                                                            | Note                                                                          |
| ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- |
| DX-1 | `RewardVerificationToken` docstring says "pass `clientTransactionId` to the ad network as SSV custom data", while the rewards guide's RN sample passes `token.customData`.                                                   | `react-native-purchases@10.9.1` `dist/purchases.d.ts` L117–121 vs `ad-monetization/rewards` § Manual integration | The app follows the guide (`customData`). One of the two should change.       |
| DX-2 | `AdMediatorName` / `AdFormat` / `AdRevenuePrecision` are exported as `const` objects typed `string` — a typo in a mediator name compiles.                                                                                    | `purchases.d.ts` L47–74                                                                                          | A string-literal union would catch `"admob"` vs `"adMob"` at compile time.    |
| DX-3 | `PurchasesVirtualCurrencies` is not re-exported from the package root; the return type of `getVirtualCurrencies()` has to be inferred.                                                                                       | `react-native-purchases` index                                                                                   | Minor; found while typing `readAdsBalance`.                                   |
| DX-4 | The docs say SSV cannot be enabled on Google's sample unit IDs, so there is no way to exercise the full round-trip before an AdMob account is approved — the spike is gated on a review queue outside the builder's control. | `ad-monetization/rewards`                                                                                        | A RevenueCat-hosted sandbox unit that accepts SSV would unblock day-1 spikes. |
